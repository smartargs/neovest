# Pinned dependency versions for reproducible builds.
# Anyone deploying this contract should compile with the same versions
# so the resulting .nef bytecode hashes deterministically.

neow3jVersion=3.22.1
javaVersion=17

# Gradle settings
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
